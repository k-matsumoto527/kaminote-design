# かみのて保育園サイト｜トップページ デザイン案

株式会社 A and K さま向け。提案書（`~/kaminote-proposal`）の **04 次のステップ**で約束した
「トップページを1枚だけ先にお作りし、方向性をご確認いただく」ためのもの。

- 実体は `index.html` 1枚のみ。ビルド不要でそのまま開ける。
- 画像は `img/` 配下（内訳は後述の「写真」を参照）。
- 公開先: https://k-matsumoto527.github.io/kaminote-design/

## 前提（2026-08-02 確定）

提案書を送付した結果、**2サイト案に決定**した。
つまり「保育園サイト」＋「放課後等デイサイト（かみのてKIDS・かみのてSMILEを1つに）」の2本。

このデザイン案は保育園サイトのトップで、**2サイト案でも中身は変わらない**
（提案書で2サイト案の保育園サイトは「※A案と同じ」と明記していたため）。
ただし**フッターの「グループの事業」は3サイト案前提で書いていたので修正済み**。
KIDSとSMILEを別サイトとして並べるのではなく、「放課後等デイサービス」1本にし、
その下に2事業所名を小さく添えている。**ここを3サイト案の書き方に戻さないこと。**

## 構成

提案書 02節「画面イメージ」の保育園サイト トップの11ブロックを、そのままの順で実装している。
**構成は提案書と一致させること。**ここでずらすと「間取りは決まった」という前提が崩れる。

1. ヘッダー（ロゴ／ナビ／見学CTA）
2. ヒーロー（見出し・写真）
2.5 ロゴ（**案Cのみ**。下記）
3. 帯（開園時間・施設数・対応言語）※提案書には無い追加分
4. 選ばれている3つの理由
5. かみのての保育 ― 大切にしていること
6. 園のご紹介（かみのて保育園／かみのて今渡保育園）
7. 病児保育のご案内 → 専用ページ
8. 入園案内・保育料（企業主導型／提携企業）
9. かみのてで働く人 → 採用ページ
10. お知らせ・園の日記
11. 見学・お問い合わせ／フッター

ナビとフッターは、提案書の**保育園サイト ページ一覧（全11ページ）すべてに導線がある**状態に
してある。「私たちについて」と「園での生活」はトップに対応するブロックが無い独立ページなので、
リンクだけ置いている（`href="#"`）。
**セクションラベルに「わたしたちについて」を使ってはいけない。**
提案書では「私たちについて」が別の独立ページ（理念・園長メッセージ・法人概要）なので、
4番目のブロックのラベルは「かみのての保育」にすること。

ナビとロゴとCTAは一列に入らなくなると折り返して崩れるので、幅を切ってメニューごと畳んでいる。
**畳む幅は案ごとに違う**（ナビの文字数と実ロゴの幅で決まる）:

| | 畳む幅 | 崩れ方 |
| --- | --- | --- |
| 案A | `max-width:1100px` | 園名が「かみの…」と省略される |
| 案B | `max-width:1000px` | ナビが2行に折り返して園名に重なる |

**この崩れは `scrollWidth` の検査では出ない**（折り返しははみ出しではないため）。
ヘッダーは幅を変えて目視すること（`kmtest/_head.html?p=n2/plan-a.html&w=901,1000,1100,1101`）。
**畳む幅の指定は、スマホ用のブロック（最大900px）とは別の `@media` に分けてある。**
まとめると 900〜1100px がスマホ用の1カラムになってしまう。

## 実ロゴ（2026-08-22 追加）

顧客からいただいた かみのて保育園のロゴ（円形バッジのスクリーンショット 329×228）から、
`img/` に2枚を起こしてある。**作り直すときは `python3 ~/kmtools/mklogo.py`**
（元画像は `~/kmtools/logo-src-nursery.png`、Pillow が入らないので `~/kmtools/png.py` の自作デコーダを使う）。

| ファイル | 中身 | 置いている場所 |
| --- | --- | --- |
| `img/logo-mark.png` | 文字（かみのて保育園）を消して絵柄だけにしたもの 313×192 | 案A・案Bのヘッダー |
| `img/logo.png` | 文字入りのまま 313×201 | 案Cのヒーロー直下 |

- **ヘッダーには文字を消したほうを使う。**隣に「かみのて保育園」と書いてあるので、
  文字入りだと園名が二重になる。**放課後等デイの `logo-mark.png` と同じ考え方。**
- 文字は左右の手の内側に収まっているので、**矩形1発（x 80〜248 / y 0〜75）で切り出せる**。
  放デイのように二段構えにする必要はなかった。
- **横長なので `height` で指定する**（PC 52px／スマホ 38px）。`width` を決め打ちすると潰れる。
- **背景の白を透明にしてあるので、手のひらのハートと顔の白も抜ける。**
  濃い色や写真の上に置くと地色が透けて読めなくなる（実際に試した）。
  **必ず白〜ごく薄い面の上に置くこと。**フッターは地色が濃いので置けない
  ＝仮マークごと外して名前だけにしてある。
- **案Cは 2026-08-22 に作り直して白い帯のヘッダーになった**ので、案A・案Bと同じく
  ヘッダーにも文字なしを置いている。写真のすぐ下の白い `.logos` はそのまま残し、
  そちらは文字入りのほう＝帯のマークと文字が二重にならないようにしてある。

## デザインの意図

### 色（2026-08-20 変更：パステルブルー）

**主色はパステルブルー。ユーザーから色見本 `#dcedf8` の指定を受けた。**
見本はとても淡く、そのままでは文字にも線にも使えないので（白地で読めない）、
**「濃くしていい」との指示を受けて濃い段を足し、3案とも同じ階調にしてある。**

- `--blue` #5fa8cc（面・線・ロゴ）／ `--blue-d` #2c6f92（文字。白地でコントラスト4.9）
  ／ `--blue-soft` #dcedf8（薄い面＝いただいた見本そのまま）
- **薄いブルーを文字色に使わないこと。**読めない。文字は `--blue-d`。
- 案Cだけトークン名が `--brand` 系（`--brand` / `--brand-d` / `--brand-soft`）。
  中身は上と同じ値。

**地色も薄い青にしてある**（2026-08-20・「もっとパステルブルーを前面に出したい」）。
**いただいた #dcedf8 を全面に敷くのは濃すぎる**（面積が大きいと重く、白いカードとの
差が消えて、CTAのオレンジともぶつかる）。**4段のはしごにして面積で振り分ける**:

| 段 | 色 | 使うところ |
| --- | --- | --- |
| 1 | #fff | カード（写真・情報が乗る面） |
| 2 | #eef7fd | 地色（`--ground`。案Bは `.head` の半透明も同じ色に） |
| 3 | #dcedf8 | 帯のセクション＝いただいた見本（案A `--cream` / 案B `--cream` / 案C `--paper`） |
| 4 | #cbe2f3 | 帯の上に置く面・ピル（`--blue-panel`） |

**トークン名は履歴のまま `--cream` / `--paper` を使っている**（中身は薄青）。
名前で暖色だと判断しないこと。**4段目が要る理由**は、帯のセクションの上に
3段目と同色の面を置くと消えるため（案Cの `.notice` と `.steps li` が実際に消えた）。
最初 #f6fbfe で試したが**白と見分けがつかず**、一段上げて #eef7fd にしている。

**CTAはグループ共通のオレンジ**（`#ef8b4c` / グラデーションは `#f8b478→#f19a4d`）。
主色を青にしたので、ボタンまで青いと押す場所が沈む。放課後等デイサイト
（パステルグリーン＋オレンジ）と対になり、**2サイトで同じ配色の考え方**になる。

**副の色でぶつかったところ**（青が主色になったことで起きた玉突き）:
- 案A：病児保育のパネルが青緑（#eef7f6）で主色と同系に沈むので暖色に振り替えた。
  「別の用件だと分かるように色を分ける」という当初の意図を保つため。
- 案B：副の「空」#8ec9dd を廃止。数字の3色目はたまご色、お知らせのタグは桃に。
- 案C：副の「空」を廃止し、数字の3色目をオレンジに。あわせて `.tab-pink` は
  実態と合わなくなったので `.tab-soft` に改名した。

**元はアプリコット／オレンジだった**（さらに前はミント #7fc6a0）。
放デイのパステルグリーンと見分けるためにミントから振り替えた経緯があるので、
**ミントにも戻さないこと。**パステルブルーは放デイのグリーンとも十分に離れている。

### 共通の設計言語

**かみのてKIDSサンプル（`~/kids-support-sample`）とテイストを揃えている。**
放課後等デイサイト（`~/kaminote-day-design`）とも、色以外はすべて共通にしてある。
同じグループだと分かるようにするため。

- 書体は Zen Maru Gothic を全面に。英字は Baloo 2（`.en`）
- ヒーローは**写真を全面背景にしてクリームのグラデーションを重ねる**方式。
  中央寄せ・ピル型バッジ・見出しの一部にマーカー（`.hl`）・2行目を主色（`.warm`）
- ヒーロー下の波型SVG（`.wave`）で次のセクションへつなぐ
- セクションラベルは絵文字＋主色の小見出し、見出し下に英字（`.sec-title .en`）
- 丸いピル型ボタン、やわらかい影、大きめの角丸
- スクロールで要素がふわっと出る（`.reveal` ＋ IntersectionObserver）

`prefers-reduced-motion` を指定している人にはアニメーションを出さない。

**ヒーローに絵文字は入れない。** サンプルには浮遊する絵文字の飾り（🌷⭐🧸🎈）があり
一度は入れたが、指示により削除した。CTAボタンに付けていた 🌱 も同時に外している。
ヒーロー以外のセクションラベルの絵文字はそのまま残している。

## 事実として使ってよい情報（提案書で確認済み）

- かみのて保育園／かみのて今渡保育園の2園＋病児保育室
- 企業主導型保育園、提携企業の枠がある
- 開園 7:00〜20:00
- 4か国語対応、年間休日120日以上
- 運営：株式会社 A and K（**「かみのてグループ」という法人は存在しない**）

## 仮の情報（取材で必ず差し替える）

住所・対象年齢・定員・電話番号・お知らせの見出し・すべての写真。
ページ最上部の黒い帯（`.demobar`）でその旨を明示している。**納品時は削除する。**

## 写真

すべて Unsplash から取得したイメージ写真。
Unsplash License なので商用利用・改変可、クレジット表記は任意。
ただし**すべて取材時に園の実写へ差し替える前提**で、
借り物と分かるよう写真の左上に「イメージ写真」バッジ（`.photo .stock`）を出している。
**納品時はバッジごと削除する。**

| ファイル | 用途 | Unsplash photo id |
|---|---|---|
| `hero-toys.jpg` | ヒーロー（使用中） | `1558877385-81a1c7e67d72` |
| `hero-field.jpg` | 旧ヒーロー（未使用・残置） | `1527490087278-9c75be0b8052` |
| `hero.jpg` | 旧ヒーロー（未使用・差し戻し用に残置） | ユーザー提供（提案書の表紙と同じ） |
| `policy.jpg` | かみのての保育 | `1587616211892-f743fcca64f9` |
| `park-1.jpg` | かみのて保育園 | `1594608661623-aa0bd3a69d98` |
| `park-2.jpg` | かみのて今渡保育園 | `1516627145497-ae6968895b74` |
| `news-1.jpg` | 夏まつり | `1509909756405-be0199881695` |
| `news-2.jpg` | 泥んこあそび | `1503454537195-1dcabb73ffb9` |
| `news-3.jpg` | 入園受付 | `1503919545889-aef636e10ad4` |
| `byoji.jpg` | 病児保育 | `1478476868527-002ae3f3e159` |

取得は `https://images.unsplash.com/photo-<id>?w=<幅>&q=78&fm=jpg&fit=crop`。
**検索API（api.unsplash.com / unsplash.com/napi）はキーが無いと401になる**ので、
候補IDを直接叩いて取得し、コンタクトシートに並べて目視で選んでいる。

**ヒーローに人を写さない。** 実在しないこどもの写真を園のトップに大きく出すと、
自園のこどもと誤解されうるため、指示により**人の写っていない写真**にしている。
木のおもちゃなど、園の空気が伝わるモチーフを使うこと。

**過去の差し替え経緯**: 旧 `hero.jpg`（製作あそびの真上からの寄り）は、
全面背景にすると絵柄が細かく色数も多いため、上に乗る文字が読めなくなる。
オーバーレイを濃くすると今度は全体が白茶けてサンプルの軽さが失われた。
全面背景に耐える（低コントラストな面がある）写真に替えている。
差し替えは `.hero` の `url()` を変えるだけでよい。

**採用情報のブロックには写真を入れていない。** 「保育士とこども」の使える写真が
Unsplash 側で見つからなかったため（候補は年代の古い米国の教室、小学校の風景、
文脈のないポートレートばかりだった）。数字タイルだけで成立しており、
テイストの元にした kids-support-sample もヒーロー以外に写真を使っていないので不整合はない。
**取材で職員の写真を撮ったら、ここに入れるのがいちばん効く。**

3つの理由・入園案内・お問い合わせも写真なし。絵文字と色面で構成しており、これは意図どおり。

**園舎の外観写真はあえて使っていない。** 実在する別の建物を「かみのて保育園」として
見せると誤解を招くため、園のようすの写真を当てている。

## 確認済みの表示

Edge ヘッドレスで検証。横スクロールは 390 / 489 / 768 / 1200px すべてで発生なし。
390px は Edge の最小ウィンドウ幅（約477px）に丸められるため、
390px 幅の `<iframe>` に入れて内側から `scrollWidth` を測っている。

`.reason` はスマホで「アイコン＋本文」の横並びにするが、
見出しと本文を `.reason-body` でくくらないと2カラムグリッドの別セルに落ちて
はみ出す。まとめてあるのはそのため。

## 下層ページに分けた試作（b/・2026-08-23）

「トップのボリュームが多い」という指摘を受けて、**保育園・案Bだけ**を
メニューごとの独立ページに分けた試作。`b/` 以下に8ページある。

- 公開: https://k-matsumoto527.github.io/kaminote-design/b/
- **1枚もの（`plan-b.html`）には手を入れていない。** 3案の見比べはそのまま残す。
  試作はあくまで「分けるとどうなるか」を見せるためのもの。

**トップに残したもの**（ユーザーが選んだ「入口だけ残す」案）:
ヒーロー／数字の帯／選ばれている3つの理由／各ページへの入口カード／
お知らせ新着3件（見出しだけ）／見学のCTA。

**別ページにしたもの7つ**: `policy` `parks` `byoji` `admission` `recruit` `news` `contact`。

**効果**: スマホ幅（390px）でのトップの高さが **10,957px → 6,042px**（約45%減）。

### 作りの約束

- **CSSは `b/style.css` に1本化**して8ページで共有する。`plan-b.html` の `<style>`
  2つ分をそのまま出したもの＋下層用の部品（パンくず・入口カード・お知らせ一覧）。
  **1枚ものと同じ見た目を保ちたいので、色や余白をここで勝手に変えないこと。**
- ヘッダーとフッターは各ページに複製してある（静的HTMLなのでテンプレート機構は持たない）。
  **文言を直すときは8ページとも直す。**
- 画像は `../img/` を参照する（`b/` の1つ上）。
- **言語切り替え（日本語/English/Português）はこの試作には入れていない。**
  構成が固まってから足す。そのため `data-i18n` 属性も落としてある。
  入れるときは `~/kmtools/i18n_mark.py` を8ページにも掛け直し、辞書を作り直すこと
  （鍵番号は日本語の内容から決まるので、対象ファイルが増えると番号がずれる）。
- リンクは全ページ実在を確認済み。**フッターのリンクは `#policy` のような
  トップ内リンクのままになりやすい**ので、ページを足したら必ず貼り直すこと。

### 2026-08-23 追記：6案すべてを分割版にした

試作（案Bのみ）がよいと確認が取れたので、**保育園3案・放デイ3案すべて**を
同じ形に分けた。生成は `python3 ~/kmtools/split_plan.py <nursery|day> <a|b|c>`。

- 保育園: `a/` `b/` `c/`（トップに残すのは「3つの理由」）
- 放デイ: `a/` `b/` `c/`（トップに残すのは「こんな方へ」）
- **提案資料のリンクは分割版（`…/kaminote-design/b/` の形）に差し替え済み。**
  1枚もの `plan-*.html` は残してあるが、資料からはもう参照していない。

**split_plan.py の勘所**:
- **id の無い節を落とさないこと。** 放デイには id 無しの節（多言語での支援）がある。
  節の数を数えて突き合わせ、表記が決まっていない節があれば止まるようにしてある。
- **案Cはヘッダー右に丸い「採用情報」バッジがある。** メニューにも入れると二重に出るので、
  `.pill-recruit` があるときは nav から recruit を外している。
- 下層用の部品（パンくず・入口カード・お知らせ一覧）は**トークン名に依存させない**。
  案ごとに `--blue-d` / `--brand-d` / `--pg-deep` とバラバラなので、`--x-accent` に寄せ、
  面の色は `rgba(0,0,0,.035)` で作る。どの配色でも成立する。

### 2026-08-24 スマホのメニューボタンを動くようにした

**それまでハンバーガーは押しても何も起きなかった。** ボタンとCSS（畳んだら `.gnav{display:none}`）
はあったが、**開閉のJSがどの案にも一度も書かれていなかった**。6案の1枚もの・分割版とも同じ状態。

- 開閉は `.head` に `open` を付け外しする方式。**指定は media query の外に書く**
  ＝畳む幅が案ごとに違う（案A 1100 / 案B 1000 / 案C 1180、非日本語は1300）ため、
  幅ごとに書くと6通り書くことになる。ボタンは畳まれた幅でしか出ないので、これで足りる。
- **開いたメニューの末尾に「見学・ご相談」を複製して入れている。** スマホではヘッダーの
  CTAが隠れるので、入れないとメニューからお問い合わせへ行けない。
- 外側クリック・項目クリック・Escで閉じる。**幅を広げてボタンが消えたら open を外す**
  （持ち越すとPCでメニューがパネルのまま残る）。
- 直すのは `plan-*.html` 側。分割版は `split_plan.py` で作り直せば反映される。
- **2026-08-24 追記: 全画面のパネルから、ボタンの下に出る小さなドロップダウンに変えた**
  （「余白が気になる、内容が見える範囲でもう少し小さく」との指摘）。
  幅は中身なり（`min-width:190px` 〜 `max-width:min(74vw,290px)`）、右端から12px、角丸14px。
  行の余白は `14px 24px → 10px 16px`、文字は `15px → 13.5px`、アイコンは `20px → 17px`。
  **画面を覆わないので、後ろのページが見えたまま選べる。**

### 2026-08-24 「園での生活」ページを足した（3案とも）

提案書02節のページ一覧にあって、トップに対応する節が無かったページ
（**1日の流れ／年間行事／園児の過ごし方**）。参考として受け取ったポポラー
（https://popolarhoiku.com/curriculum/ ）の「園での一日&カリキュラム・行事」を下敷きに、
かみのてさんの強み（開所7:00〜20:00・看護師常駐・多言語）が出る形に組み替えている。

**本文の原本は `~/kmtools/pages/nursery-life.html` 1本だけ。** 3案とも同じものを使い、
`split_plan.py` がヘッダー・フッター・配色をかぶせて `a/life.html` `b/life.html` `c/life.html`
を出す。**文言は3案とも同一・見た目の差は style.css** という約束をそのまま守れる形にした。

- **原本に案ごとの色を書かないこと。** トークン名が案ごとに違う（`--blue-d` /
  `--brand-d`）ので、`--x-accent` と薄い黒（`rgba(0,0,0,...)`）だけで組む。
- **写真は `.x-ph`**。案Aは `.ph`、案B・Cは `.ph-round` と名前が違うので、共通の受け皿を
  `split_plan.py` の EXTRA_CSS 側に置いた。改行の制御も `.br-sp` は案Aにしか無いので `.x-br`。
- **帯にする節は `class="sec x-alt"` と書く。** 案A `tint` / 案B `white` / 案C `alt` と
  呼び名が違うので、生成時に貼り替えている。
- 中身は 一日の流れ（**0・1・2歳と3・4・5歳の2本立て**）／大切にしている時間（6つ）／
  年間行事（12か月＋毎月あること）。**時間と行事名はほぼ仮**で、確かなのは開所時間だけ。
  ページ末尾に仮である旨の注記を入れてある。

### 2026-08-24 分割版はメニューを畳む幅を取り直した

「園での生活」をメニューにも入れて**項目が7→8に増えた**ため、1枚もののままの幅では崩れる。
**1枚もの（`plan-*.html`）の指定（案A1100 / 案B1000 / 案C1180）は動かさず**、分割版だけ
`split_plan.py` の `FOLD` で一段広い幅から畳むようにしてある。

| | 分割版の畳む幅 | そこから下で起きる崩れ |
| --- | --- | --- |
| 案A | 1209px | 園名が「かみの…」と省略される |
| 案B | 1140px | メニューの項目が2行に折り返す |
| 案C | 1180px | （1枚ものと同じ。採用情報が丸バッジで外に出ているぶんメニューが1項目少なく、余裕がある） |

**幅は目で決めずに測った。** `~/kmtools/_head2.html` が幅ごとに
「園名のうしろのすきま／メニューの高さ（＝折り返し）／園名が省略されたか／ボタンとのすきま」
を数字で出す。**畳む幅＝すべてOKになるいちばん狭い幅 −1**。

- **畳む指定はハーネス側で打ち消してから測る。** 打ち消さないと畳まれた状態を測ってしまい、
  どの幅でも「OK」に見える。
- **項目が2行になる崩れは、メニュー全体の高さでは検出できない**（高さがそろって伸びるため）。
  1行ぶんの高さ（34px）で直接見ること。案Bはこれで見つけた。
- この崩れはどれも `scrollWidth` の検査には出ない。

### 2026-08-24 分割版のファビコンが全ページ壊れていたのを直した

`split_plan.py` が `<link rel="icon">` を `[^>]*>` で拾っていたため、**データURIの中の
`<svg ...>` の `>` で切れて**、`href` が途中までのゴミになっていた（48ページとも）。
`[^\n]*">` に直した。1枚ものは無事だったので、分割版に切り出した時点からの不具合。

### 2026-08-24 「よくあるご質問」ページを足した（いまは案Aだけ）

参考は https://popolarhoiku.com/faq/ （開閉式・カテゴリ分けなし）。
質問が16件あるので**5つに分けた**（入園・お申し込み／園での生活／保育料・費用／
健康・安全／ことば・文化）。分けないと探せない。

- 原本は `~/kmtools/pages/nursery-faq.html`。**2026-08-24 に案B・案Cにも広げた**
  （`plans=["a"]` を外した）。そのとき畳む幅を測り直している＝案B 1140→**1219**、
  案C は 1180 のまま（採用情報が丸バッジで外に出ているぶんメニューが1項目少なく、
  8項目でも収まる）。**案Bは項目そのものが2行に折り返していた**＝`.gnav a` に
  `white-space` の指定が無く「よくある／ご質問」で割れる。折り返しを止めたうえで
  間隔を 22→16px に詰め、頭打ちを 1200→1280px に上げた（`HEAD_CSS`）。
- **開閉は `<details>`/`<summary>`。JSは足していない。** キーボードでも開け、
  検索にも中身が乗る。`summary` は `display:flex` ＋ `list-style:none` ＋
  `::-webkit-details-marker{display:none}` で三角を消し、右端の山かたは `::after` で描く。
- 導線は4か所＝**メニュー／トップ下段のボタンの並び／フッター／入口カード**。
  ボタンの並び（見学を申し込む・お問い合わせ・よくあるご質問）は `EXTRA` の
  `cta_top` から生やしている。フッターは枠が無かったので `foot_after` で
  「お知らせ・園の日記」のうしろに1行足している。
- **答えのうち事実は DATA.md にあるものだけ**（開所時間・保育料・送迎・一時預かり・
  病児保育・対応言語）。慣らし保育・持ちもの・与薬・防犯などは一般的な例で書いた仮の
  もので、末尾に仮である旨を書いてある。

**★ 案Aは9項目でメニューが1280pxに収まらなくなった。**
`.head-in` が `max-width:1280px` で頭打ちなので、**画面をいくら広げてもメニューに使える
幅は増えない**（1440pxでも園名が「かみのて保育…」と省略されたままだった）。
項目のあいだを **26→20px** に詰めて 8か所×6px＝48px を作り、43px 足りない分を吸収した。
この詰めは案Aの分割版だけに効かせている（`split_plan.py` の `HEAD_CSS`）。
畳む幅は **1209→1274px**。`_head2.html` は「あと何px足りないか」も出すようにした
（`name=cut(-43)` の形）。詰め方を決めるのにこの数字が要る。

### 2026-08-24 トップのお知らせをヒーローの直下に移した

ユーザー指示。**分割版の3案とも**、並びを ヒーロー → **お知らせ** → 数字の帯 →
3つの理由 → 入口カード → 見学のCTA に変えた（`split_plan.py` の `parts`）。
いちばん新しい情報が最初に目に入るようにするため。

- **見出しは「お知らせ」1語**にした（`NEWS`。それまでは小見出し「お知らせ・園の日記」＋
  「園のいま、おとどけします」＋`NEWS & DIARY`）。小見出しは「お知らせ」と重なるので外した。
- **下層の `news.html` の見出しは「園のいま、おとどけします」のまま。** あちらは
  `plan-*.html` 側の節をそのまま切り出したもので、ほかの下層ページの見出しとも
  作りをそろえてある。変えるなら1枚もの3案＋`i18n.js` の k084 も直すことになる。
- **1枚もの（`plan-*.html`）の並びは変えていない。** 3案の見比べはそのまま。

### 2026-08-24 採用の窓口を新卒／中途に分けた（3案とも）

「新卒と中途で訴求する内容が変わる」というユーザー指示。**`recruit.html` は窓口のまま残し**、
その下に `recruit-new.html`（新卒採用）と `recruit-mid.html`（中途採用）を置いた。

- **窓口ページの死んだボタンを割った。** `recruit` の節には
  `<a class="btn btn-main" href="#">採用情報を見る</a>` が入っている（3案とも同じ文字列）。
  1枚もののトップでは誘い文句だが、**下層に切り出すと押しても何も起きないボタン**になっていた。
  これを「新卒採用の方へ」「中途採用の方へ」の2つに置き換えている
  （`split_plan.py` の `UNDER_REPLACE` と `EXTRA` の `under` / `under_btn`）。
- **メニューには足していない。** 案Aは9項目で `.head-in` の 1280px 上限に達しており、
  これ以上は増やせない。窓口は「採用情報」1つのままにして、そこから2つに分ける。
- **入口カード（トップ）にも出さない。** 「採用情報」と並べると窓口が3つに見えるので、
  `under` の付いたページはカードから除いている。フッターには
  「採用情報」の下に「新卒採用」「中途採用」の2行を足した（`foot_after` の連鎖）。
- **中身の書き分け**: 新卒＝1年目の育ち方（入職まで／入職後の2本立て）・選考の流れ。
  中途＝ブランク・両立・多言語の3点、働き方2つ（正職員／パート・時短）、職種別の募集要項、
  入職までの流れ、「話だけ聞いてみたい、でもかまいません」の囲み。
- **給与・待遇の数字は書いていない。**「取材時に確認して記載します」と置いてある。
  分からないものを埋めると嘘になる。**写真も入れていない**（使えるストック写真が
  見つかっていないため。上の「写真」の節と同じ理由）。
- 募集要項の表は `.x-dl`。**560px以下では項目名を上に回す**（表のまま縮めると項目名が縦に潰れる）。

### 2026-08-24 保育理念のページを新設した（3案とも）

提案書02節のページ一覧にある「**私たちについて**」（私たちの想い／保育理念・保育方針／
園長メッセージ／法人・園の概要）にあたるページ。原本は
`~/kmtools/pages/nursery-philosophy.html`、URLは `philosophy.html`。

- **`policy.html`（かみのての保育）と役割を分けること。**
  こちら＝**理念・方針・目標・園長のことば**（上位の考え方）。
  あちら＝**待つ保育／ちがいをあたりまえに／家庭とチーム**（日々の関わり方）。
  同じことを2ページで言わない。両ページから相互にリンクを張ってある。
- 中身は 理念（1文の板 `.creed`）／保育方針3つ／保育目標4つ／園長メッセージ `.msg`／
  園・法人の概要 `.x-dl`。**写真は入れていない**（人物写真の借り物を理念ページに置かない）。
- **理念・方針・目標・園長メッセージは仮の案。** トップの見出し
  「こどもの『やってみたい』を、まんなかに。」と既存ページから逆算して組んだもので、
  末尾にその旨を書いてある。**すでに理念がおありなら差し替える。**
  園・法人の概要（園長名・定員・開所時間・運営会社）は現行サイトからの引用。
- **メニューには入れていない。** 案A・案Bは9項目で `.head-in` の幅の上限に達している。
  導線は入口カード（「かみのての保育」の次）とフッター（同）から。
- `.creed-t` は**スマホで20pxまで落としている**。落とさないと理念の1行目（14字）が
  収まらず「こどもの『やってみたい』／を、／まんなかに。」と「を、」だけが行に残る。

### 2026-08-24 メニューに保育理念を足した（3案とも）

「かみのての保育」の**前**に置いている（理念が上位なので順番を逆にした。`nav_before`）。
これで 案A・案B は10項目、案C は9項目（採用情報は丸バッジで外に出ている）。

| | 畳む幅 | メニューの詰め（分割版だけ） |
| --- | --- | --- |
| 案A | 1273px | 項目の間隔 26→14px、文字 13.5→13px |
| 案B | 1269px | 折り返し禁止、間隔 22→14px、頭打ち 1200→1280px |
| 案C | 1180px | なし（1170まで収まるが、下の★の理由でこれ以上下げられない） |

- **★分割版の畳む幅は、1枚もの側の指定より下げられない。** こちらの `@media` は
  あちらに重ねて範囲を広げるだけで、打ち消せないため。案Cは1枚ものが1180pxなので、
  測って1170まで収まると分かっても1180のまま（1172pxで確かめたら畳まれていた）。
- **★`_head2.html` の判定を直した。1pxの食み出しでも省略記号は出る。**
  `scrollWidth > clientWidth + 1` としていたため、**実際には「かみのて保育…」と
  切れている状態を「収まっている」と報告していた**（scrollWidth=171 / clientWidth=170）。
  `+1` を外し、畳む幅は「収まるいちばん狭い幅 +2px」で置くことにした。
  測定と見た目が食い違ったときは `kmtest/_probe.html?p=n2/a/index.html&w=1270` で
  同じ描画の上に数値を出して突き合わせる。

### 2026-08-24 案Aのヘッダーとヒーローを作り替えた

ユーザーが参考として Green House 新大阪園のトップ画面を提示。
**ヘッダーとヒーローだけ**をそのイメージに寄せた（下の節と下層ページはそのまま）。

- **写真の上下を雲形で切る。** ★`border-radius` で切り抜くのではなく、
  **同じ色の面（SVG）を上下にかぶせて切れて見せている**。大きな写真を四角でない形で
  切り抜くと、要素が画面を通過するあいだ合成し直しになってスクロールが重くなる
  （案Cで実際に踏んだ）。塗るだけなら安い。上は白（ヘッダーとつながる）、
  下は `--ground`（次の節と同じ色）。
- **ヘッダーの下線を外した。** 線があると雲形が途中で切れて見える。
  スクロールで本文の上に来たときだけ影で分かるようにしてある。
- **左下に木のイラスト**（`.hero-trees`）、右上の粒（`.hero-cluster`）はそのまま。
- **右端に貼りつく縦のボタン**（`.side-cta`＝見学のお申し込み／病児保育）。
  参考イメージの「園見学予約」「病児・病後児保育」にあたる。
  **★1200px を切ると本文（最大1120px＋余白）に重なるので出さない。**
  900px で消す指定だと 901〜1200px で文字の上に乗る。
  **文言は既にある鍵を使い回している**（k019 / k006）。新しい鍵を足すと
  `i18n_mark.py` を掛け直したときに番号がずれるため。
- **`split_plan.py` が `.side-cta` を全ページへ複製する**（`kind()` に "side" を追加）。
  これを入れないと、分割版には出ない（トップ直下の div は捨てられていた）。
- **見出しは濃い文字のまま**（参考イメージは白抜き）。この写真は明るいパステルで、
  白文字にするとコントラストが 3.20 まで落ちる（測定済み）。**明るい写真に白文字は不可。**
- **明朝の見出しも残してある。** 3案の見分け（案A＝明朝、案B・案C＝まるゴシック）が
  つかなくなるため。変えるならユーザー判断で。

### 2026-08-25 分割版でも言語を切り替えられるようにした

**それまで言語ボタンは1枚もの（`plan-*.html`）にしか無く、資料からお客様が開く分割版は
日本語だけだった。** 資料には「English / Português を押すと切り替わります」と書いてあり、
食い違ったままになっていた。

- `split_plan.py` を3か所直した＝`clean()` で `data-i18n` を落とすのをやめる／
  ヘッダーの言語ボタンを消すのをやめる／各ページに `../i18n.js` を読ませる。
- **印付けは「生成したあと」に行う。** メニュー・フッター・入口カード・お知らせは
  `split_plan.py` が組み立て直すので、原本に印を付けても付いてこない。
  **手順は `python3 ~/kmtools/split_plan.py <site> <案>` ×3 → `python3 ~/kmtools/i18n_apply.py <site>`。**
- **★訳は鍵の番号ではなく「日本語の中身」で引き当てて持ち越す。** 番号は日本語の初出順で
  決まるので、文を1つ足すだけで全部ずれる。手で書いた訳は
  `~/kmtools/i18n-manual-<site>.json`（日本語 → {en, pt}）に置き、`i18n.js` は毎回作り直す。
  **`i18n.js` を直接編集しないこと**（次の生成で消える）。
- 新しく足した5ページ分もすべて訳した（保育園 458 件／放デイ 455 件、未訳ゼロ）。
  同じ日本語は2サイトで訳を共有している。

**ラテン文字でヘッダーが壊れる件を2つ直した**:
- **園名が幅ゼロまで潰れてロゴだけになっていた。** ヘッダーの中身は 1280px で頭打ちで、
  英語だとメニュー・言語ボタン・CTAだけでちょうど埋まり、**縮められるのが園名だけ**なので
  そこが消える。非日本語では**頭打ちを 1440px に広げ／会社名の行を隠し／項目の間隔を12pxに詰め**、
  それ未満の幅では畳む（`@media (max-width:1439px)`）。
- **メニューの表記を短くした**（Our Philosophy→Philosophy、Life at the nursery→Daily life など）。
  辞書は1本なので、入口カードやフッターの表記も一緒に短くなる。
- **`<br class="x-br">` のうしろに空白を入れる**（英語・ポルトガル語は語間が要る）。
  日本語のままの改行タグだと "fees.Please" のようにつながる。

### 2026-08-25 スマホで言語ボタンと園名が取り合いになるのを直した

**390px で園名が「かみのて…」と切れていた。** 言語ボタン（日本語/EN/PT）とロゴと
ハンバーガーで幅が埋まり、**縮められるのが園名だけ**なのでそこが消える。
言語ボタンはスマホでも隠さない方針（外国語のご家庭にはここが入口）なので、
**まわりを削って園名の幅を作る**方向で直した。

- **560px以下**: 英字の副題（KAMINOTE NURSERY）と会社名の行を隠す／ロゴ34px／
  園名15px／言語ボタンを小さく／余白を詰める。
- **400px以下**: もう一段（ロゴ30px・園名13px・ボタン10px）。360〜375px の端末向け。
- **放デイはさらに一段**（園名12px・ロゴ28px）。「かみのてKIDS・SMILE」は
  「かみのて保育園」より4文字ぶん長く、同じ設定では収まらない（`HEAD_CSS` の `SP_NAME`）。
- **★言語ボタンの文言は訳さないようにした。** 「日本語 / English / Português」と
  それぞれの言語で書いてあるのが切り替えの作法で、英語版で「Japanese」と出ると
  自分の言語を探しにくいうえ横にも長い。`i18n_apply.py` が `.lang` の中の
  `data-i18n` を落としている。

**★同じ幅を狙う @media は「狭いほう」を後ろに書くこと。** 最初は 400px の指定を
560px の前に置いてしまい、560px の指定に上書きされて**まったく効いていなかった**
（画面を見て「変わっていない」と気づいた）。案ごとの `HEAD_CSS` も同じ理由で
EXTRA_CSS の末尾に移した。

**残っている制約**: 320px（旧世代の小型端末）では、英語・ポルトガル語のときに
名前が省略記号で切れる。ポルトガル語の「Kaminote KIDS e SMILE」が長いため。
横スクロールは出ない。
